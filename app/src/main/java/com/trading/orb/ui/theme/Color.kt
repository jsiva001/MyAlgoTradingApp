package com.trading.orb.ui.theme

import androidx.compose.ui.graphics.Color

// ============ PAPER TRADING MODE (GREY) ============
// Dark Theme Colors - Paper Mode
val PaperBackground = Color(0xFF111827)
val PaperSurface = Color(0xFF1F2937)
val PaperSurfaceVariant = Color(0xFF374151)

val PaperPrimary = Color(0xFF6B7280)         // Grey
val PaperPrimaryVariant = Color(0xFF4B5563) // Darker Grey

// ============ LIVE TRADING MODE (BLUE) ============
// Dark Theme Colors - Live Mode
val LiveBackground = Color(0xFF111827)
val LiveSurface = Color(0xFF1F2937)
val LiveSurfaceVariant = Color(0xFF374151)

val LivePrimary = Color(0xFF3B82F6)          // Blue
val LivePrimaryVariant = Color(0xFF2563EB)  // Darker Blue

// ============ SHARED COLORS ============
val Success = Color(0xFF10B981)
val SuccessVariant = Color(0xFF059669)

val Error = Color(0xFFEF4444)
val ErrorVariant = Color(0xFFDC2626)

val Warning = Color(0xFFF59E0B)
val WarningVariant = Color(0xFFD97706)

val OnBackground = Color(0xFFFFFFFF)
val OnSurface = Color(0xFFFFFFFF)
val OnPrimary = Color(0xFFFFFFFF)

val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFF9CA3AF)
val TextTertiary = Color(0xFF6B7280)

// Trading specific colors
val BuyColor = Success
val SellColor = Error
val ProfitColor = Success
val LossColor = Error

// ============ DEFAULT COLORS (Paper Mode) ============
val Background = PaperBackground
val Surface = PaperSurface
val SurfaceVariant = PaperSurfaceVariant
val Primary = PaperPrimary
val PrimaryVariant = PaperPrimaryVariant

